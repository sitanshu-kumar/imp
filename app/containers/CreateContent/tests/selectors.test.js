// import { selectCreateContentDomain } from '../selectors';

describe('selectCreateContentDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
