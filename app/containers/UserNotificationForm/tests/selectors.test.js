// import { selectUserNotificationFormDomain } from '../selectors';

describe('selectUserNotificationFormDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
