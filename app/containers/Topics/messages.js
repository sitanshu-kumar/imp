/*
 * Topics Messages
 *
 * This contains all the text for the Topics container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Topics';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Topics container!',
  },
});
