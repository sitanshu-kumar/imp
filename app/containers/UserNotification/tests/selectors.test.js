// import { selectUserNotificationDomain } from '../selectors';

describe('selectUserNotificationDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
