/*
 * Interests Messages
 *
 * This contains all the text for the Interests container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Interests';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Interests container!',
  },
});
